<Page x:Class="ShortCutDeckDesktop.Resources.Icons.ProfileLister"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProfileLister">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
    <ItemsControl x:Name="profileListerItemsControl">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid x:Name="spProfileShower" MinWidth="150" MinHeight="200">
                        <Button Margin="5 5 5 5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Tag="{Binding Id}" Click="OnProfileSelected">
                        <TextBlock Text="{Binding Name}"/>
                    </Button>
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
    </ScrollViewer>
</Page>
